[project]
name = "vscode-expensetracker-mcp-servers"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sachinshettys90", email = "sachinshettyss85@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=4.0.3",
]

[project.scripts]
vscode-expensetracker-mcp-servers = "vscode_expensetracker_mcp_servers:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
